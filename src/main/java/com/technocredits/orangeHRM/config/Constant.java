package com.technocredits.orangeHRM.config;

import java.io.File;

// Declare a variable filePath and initialise the path of the property file
public class Constant {
	
	public static String filePath = System.getProperty("user.dir") + File.separator + "src" + File.separator + "main" + File.separator + "java" + File.separator + "com" + File.separator + "technocredits" + File.separator + "orangeHRM" + File.separator + "config" + File.separator;

}
